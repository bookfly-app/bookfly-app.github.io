<div></div>
<button>+</button>

<style>
	button {
		position: absolute;
		background-color: var(--lavender);
		color: var(--base);
		bottom: 6rem;
		right: 2rem;
		font-size: 3rem;
		width: 4rem;
		height: 4rem;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	div {
		position: absolute;
		background-color: var(--lavender);
		color: var(--base);
		bottom: 6rem;
		right: 2rem;
		font-size: 3rem;
		width: 4rem;
		height: 4rem;
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--crust);
		filter: blur(10px);
		border-radius: 50%;
	}
</style>
